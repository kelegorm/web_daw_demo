---
phase: 05-transport-decoupling-+-integration-close-out
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 244 Vitest unit tests pass (21 test files passed out of 23, 2 known jsdom errors)"
    - "All Playwright E2E specs pass — transport play/stop/BPM, track/mixer controls, device panel, meters"
    - "Interface is visually and functionally identical to pre-refactor"
  artifacts: []
  key_links: []
---

<objective>
Run the full Vitest + Playwright regression suite and fix any failures introduced by Phase 5 changes.

Purpose: Satisfies COMPAT-01 (all Vitest tests pass), COMPAT-02 (all Playwright E2E tests pass), and COMPAT-03 (interface identical to pre-refactor). This is the final gate before declaring Phase 5 and the entire v1.0 milestone complete.
Output: Green test suites; any fixes committed; milestone v1.0 confirmed complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-transport-decoupling-+-integration-close-out/05-RESEARCH.md
@.planning/phases/05-transport-decoupling-+-integration-close-out/05-01-SUMMARY.md
@.planning/phases/05-transport-decoupling-+-integration-close-out/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Vitest + Playwright regression pass</name>
  <files></files>
  <action>
1. Run `npm run test -- --run` and verify:
   - 21 test files pass (out of 23 — the 2 jsdom-environment files produce known ERR_REQUIRE_ESM errors, NOT new failures)
   - 244 tests pass (this is the baseline — any drop indicates a regression)
   - If test count drops, investigate and fix. Common causes after Phase 5:
     - Import path changes (useTransportController signature changed)
     - Missing exports from new context files
     - Type errors from removed props

2. Run `npm run test:e2e` and verify all specs pass:
   - `smoke.spec.ts` — basic app loads
   - `toolbar.spec.ts` — play/stop/BPM/loop/panic buttons work
   - `trackzone.spec.ts` — track selection, mute, volume, add/remove
   - `devicepanel.spec.ts` — device panel shows correct devices
   - `midi-keyboard.spec.ts` — MIDI keyboard input
   - `vumeter.spec.ts` — meters show activity
   - `sequencer.spec.ts` — sequencer steps fire
   - `sequencer-delivery.spec.ts` — note delivery
   - `integration.spec.ts` — full flow
   - `playhead.spec.ts` — playhead movement
   - `timeline-ruler.spec.ts` — ruler display
   - `panner.spec.ts` — panner controls
   - `design-tokens.spec.ts` — CSS variables
   - `remount.spec.ts` — StrictMode remount

3. If any E2E test fails, diagnose the failure:
   - **Toolbar button selectors:** E2E tests use `.toolbar-play-pause`, `.toolbar-stop`, etc. Verify Toolbar.tsx still has these class names after context migration.
   - **Transport state after play/stop:** If the play button doesn't respond, check that TransportProvider's `actions.toggle` is wired correctly.
   - **Track mute sync:** If muting a track doesn't reflect in transport state, check that `transportActions.setTrackMute(muted)` is called in TrackZone when DEFAULT_TRACK_ID is muted.
   - **Panic count:** If `window.__panicCount` is not incrementing, check that the panic handler in Toolbar updates it.
   - **Active steps:** If `window.__activeSteps` is not populated, check that TransportProvider has the useEffect that tracks `transport.currentStep`.

4. Fix any regressions. Each fix should be minimal and targeted — do not refactor unrelated code.

5. After all tests are green, run `npm run build` one final time to confirm production build succeeds.

**Known noise to ignore:**
- 2 "Unhandled Error" entries from jsdom@28/html-encoding-sniffer ERR_REQUIRE_ESM — these are pre-existing and documented
- "Test Files 21 passed (23)" — the (23) includes the 2 error files, this is expected
  </action>
  <verify>
    `npm run test -- --run` output shows "Tests 244 passed" and "Test Files 21 passed"
    `npm run test:e2e` output shows all specs passed
    `npm run build` succeeds
  </verify>
  <done>
    All 244 Vitest tests pass. All Playwright E2E tests pass. Production build succeeds. COMPAT-01, COMPAT-02, COMPAT-03 satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 complete: Transport decoupled from TrackStripHook, TransportContext created, Toolbar and TrackZone migrated to context consumers, full regression suite green. This completes the v1.0 milestone.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. Click Play — sequencer should start, playhead moves, step indicators light up
    3. Click Pause — playhead freezes at current position, step preserved
    4. Click Play again — resumes from paused position
    5. Click Stop — playhead returns to start, step resets to -1
    6. Drag BPM up/down — tempo changes audibly and visually
    7. Toggle Loop — loop indicator and playhead behavior change
    8. Click Panic — notes cut off immediately
    9. Click track Mute button — track audio silences, sequencer continues stepping
    10. Adjust track volume slider — gain changes
    11. Click Add Track — new track appears with working meters
    12. Click Remove Track — track disappears, selection moves
    13. All visual layout identical to before refactor — no visual regressions
  </how-to-verify>
  <resume-signal>Type "approved" if the app works identically to pre-refactor, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run test -- --run` — 244 tests pass
2. `npm run test:e2e` — all specs pass
3. `npm run build` — succeeds
4. Manual verification — app works identically to pre-refactor
</verification>

<success_criteria>
- COMPAT-01: All Vitest tests pass (244 tests, 21 files)
- COMPAT-02: All Playwright E2E tests pass
- COMPAT-03: Interface visually and functionally identical to pre-refactor
- Phase 5 complete, v1.0 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-transport-decoupling-+-integration-close-out/05-03-SUMMARY.md`
</output>
