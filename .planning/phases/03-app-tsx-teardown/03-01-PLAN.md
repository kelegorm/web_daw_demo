---
phase: 03-app-tsx-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/Layout.tsx
  - src/App.test.tsx
  - src/hooks/useTrackSelection.ts
  - src/hooks/useTrackSelection.test.ts
autonomous: true

must_haves:
  truths:
    - "App.tsx contains only DawProvider wrapping Layout -- no useState, no useEffect, no hook calls, no module-level graphs"
    - "useTrackSelection.ts does not exist in the codebase and nothing imports it"
    - "useAudioEngine.ts does not exist in the codebase and nothing imports it"
    - "All Playwright E2E tests pass unchanged -- UI behaves identically to before"
    - "buildUiRuntime.ts is completely unchanged -- no modifications to the file"
  artifacts:
    - path: "src/App.tsx"
      provides: "Minimal shell: module-level DawStore + DawProvider wrapping Layout"
      max_lines: 20
    - path: "src/components/Layout.tsx"
      provides: "All former App.tsx internals: module-level graphs, hook calls, model assembly, actions, JSX"
      min_lines: 150
  key_links:
    - from: "src/App.tsx"
      to: "src/state/DawStore.ts"
      via: "module-level new DawStore(getAudioEngine(), initialState)"
      pattern: "new DawStore"
    - from: "src/App.tsx"
      to: "src/context/DawProvider.tsx"
      via: "DawProvider store={_store}"
      pattern: "DawProvider"
    - from: "src/App.tsx"
      to: "src/components/Layout.tsx"
      via: "Layout rendered as child of DawProvider"
      pattern: "<Layout"
    - from: "src/components/Layout.tsx"
      to: "src/ui-plan/buildUiRuntime.ts"
      via: "buildUiRuntime call in Layout function body"
      pattern: "buildUiRuntime"
    - from: "src/components/Layout.tsx"
      to: "src/engine/engineSingleton.ts"
      via: "module-level getAudioEngine() for graph wiring"
      pattern: "getAudioEngine"
---

<objective>
Gut App.tsx to a minimal shell that creates a module-level DawStore and renders `<DawProvider store={_store}><Layout /></DawProvider>`. All current App.tsx internals (module-level device graphs, hook calls, model assembly, action callbacks, window globals, JSX) move verbatim into a new Layout.tsx component.

Purpose: Satisfies APP-01 (App.tsx gutted), APP-02 (useAudioEngine already deleted -- verify), APP-03 (useTrackSelection deleted), APP-04 (buildUiRuntime kept unchanged, callsite moved to Layout). This is the structural prerequisite for Phase 4's component-by-component context migration.

Output: Rewritten App.tsx (~15 lines), new Layout.tsx (~200 lines), three files deleted (App.test.tsx, useTrackSelection.ts, useTrackSelection.test.ts).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-app-tsx-teardown/03-RESEARCH.md
@src/App.tsx
@src/state/DawStore.ts
@src/context/DawProvider.tsx
@src/state/defaultState.ts
@src/hooks/useTrackSelection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Layout.tsx and rewrite App.tsx</name>
  <files>
    src/components/Layout.tsx
    src/App.tsx
  </files>
  <action>
**Step A: Create `src/components/Layout.tsx`**

Move ALL content from App.tsx into Layout.tsx, adjusting import paths from `./` to `../` since Layout lives one directory deeper. Specifically:

1. **Module-level code (lines 35-114 of current App.tsx):** Move the `declare global` block, `INITIAL_TRACK_ID` constant, `_synthGraph`, `_pannerGraph`, wiring calls, `_singletonEngine`, `_track1Strip`, `_limiterGraph`, `_masterFacade`, `_masterStripHook`, and `legacyEngineAdapter` -- all verbatim to Layout.tsx module scope. Update import paths: `'./hooks/...'` becomes `'../hooks/...'`, `'./engine/...'` becomes `'../engine/...'`, etc.

2. **Layout function body:** Contains everything from the current `App()` function body:
   - All hook calls: `useToneSynth(_synthGraph)`, `usePanner(_pannerGraph)`, `useTrackStrip(_track1Strip)`, `useMasterStrip(_masterStripHook)`, `useLimiter(_limiterGraph)`, `useTransportController(toneSynth, trackStrip, DEFAULT_MIDI_CLIP_SOURCE)`
   - **Inline track selection** (replaces `useTrackSelection` import): `const [selectedTrack, setSelectedTrack] = useState<string>(INITIAL_TRACK_ID)` and `const selectTrack = useCallback((id: string) => setSelectedTrack(id), [])`. Import `useCallback` from React.
   - `trackRecByTrackId` useState (unchanged)
   - `buildUiRuntime(...)` call using `selectedTrack` instead of `trackSelection.selectedTrack`
   - `trackZoneModel` assembly (unchanged, use `selectedTrack` where it used `trackSelection.selectedTrack`)
   - `devicePanelModel` assembly (unchanged)
   - `trackZoneActions` assembly -- use `selectTrack` instead of `trackSelection.selectTrack`
   - Both `useEffect` hooks for `window.__panicCount` and `window.__activeSteps` (unchanged -- Playwright E2E tests observe these globals)
   - `handlePanic` callback (unchanged)
   - Return JSX: `<div id="app">` with Toolbar, TrackZone, DevicePanel, MidiKeyboard (unchanged, using `selectedTrack` for the rec-enabled prop)

3. **Import `App.css`:** Change to `import '../App.css'` since Layout is in components/.

4. **Export:** `export default function Layout() { ... }`

**Step B: Rewrite `src/App.tsx`**

Replace the entire file with the minimal shell from research Pattern 1:

```typescript
import { DawStore } from './state/DawStore'
import { DawProvider } from './context/DawProvider'
import { getAudioEngine } from './engine/engineSingleton'
import { DEFAULT_PROJECT_DOCUMENT, DEFAULT_UI_STATE } from './state/defaultState'
import Layout from './components/Layout'

const _store = new DawStore(getAudioEngine(), {
  project: DEFAULT_PROJECT_DOCUMENT,
  ui: DEFAULT_UI_STATE,
})

export default function App() {
  return (
    <DawProvider store={_store}>
      <Layout />
    </DawProvider>
  )
}
```

No useState, no useEffect, no hook imports, no module ID constants, no audio graph code.

**Critical details:**
- DawStore is created at MODULE LEVEL (not inside App function) -- React StrictMode double-mount safety
- `import './App.css'` moves to Layout.tsx as `import '../App.css'` -- App.tsx no longer needs it
- Do NOT modify `buildUiRuntime.ts` -- it stays completely unchanged
- The `MidiKeyboard enabled` prop must use `trackRecByTrackId[INITIAL_TRACK_ID]` (still referencing the constant from `resolveInitialTrackId`)
  </action>
  <verify>
1. `npx tsc --noEmit` -- zero type errors (confirms all imports resolve and types match)
2. `grep -c 'useState\|useEffect\|useToneSynth\|usePanner\|useTrackStrip\|useLimiter\|useMasterStrip\|useTransportController' src/App.tsx` returns 0 (no hook calls in App.tsx)
3. `wc -l src/App.tsx` returns under 20 lines
4. `grep 'DawProvider' src/App.tsx` finds the DawProvider wrapper
5. `grep 'buildUiRuntime' src/components/Layout.tsx` confirms the callsite moved
  </verify>
  <done>
App.tsx is under 20 lines containing only: DawStore module-level instantiation, DawProvider wrapping Layout, and necessary imports. Layout.tsx contains all former App.tsx internals with identical behavior. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete deprecated files and verify E2E</name>
  <files>
    src/hooks/useTrackSelection.ts
    src/hooks/useTrackSelection.test.ts
    src/App.test.tsx
  </files>
  <action>
**Step A: Delete three files**

1. Delete `src/hooks/useTrackSelection.ts` -- its functionality is inlined in Layout.tsx (APP-03)
2. Delete `src/hooks/useTrackSelection.test.ts` -- tests the deleted hook, no longer relevant
3. Delete `src/App.test.tsx` -- 336 lines testing App.tsx's old wiring, entirely irrelevant now that App.tsx is a 15-line shell. Consistent with prior useAudioEngine.test.tsx deletion precedent.

**Step B: Verify no dangling imports**

Run `grep -r 'useTrackSelection' src/` to confirm zero results. If any file besides the deleted ones imported from useTrackSelection, fix those imports (research confirmed only App.tsx imported it, and App.tsx no longer does).

**Step C: Run unit tests**

Run `npm run test` (Vitest). All remaining unit tests must pass. The deleted test files should not cause failures. If any other test imported from useTrackSelection or App.test.tsx, that would surface as a failure here.

**Step D: Run E2E tests**

Run `npm run test:e2e` (Playwright). All existing E2E tests must pass unchanged. This is the primary regression gate -- if the E2E tests pass, the UI behaves identically to before the teardown. The `window.__panicCount` and `window.__activeSteps` globals must be set by Layout.tsx's useEffect hooks for the E2E tests to observe them.

**If E2E tests fail:** Debug by checking whether Layout.tsx module-level graph wiring matches the original App.tsx wiring. Common failure causes:
- Missing `_pannerGraph.output.connect(_track1Strip.input)` wiring
- Missing `window.__activeSteps` or `window.__panicCount` useEffect
- Import path errors in Layout.tsx (./  vs ../)
  </action>
  <verify>
1. `ls src/hooks/useTrackSelection.ts 2>/dev/null` returns nothing (file deleted)
2. `ls src/hooks/useTrackSelection.test.ts 2>/dev/null` returns nothing (file deleted)
3. `ls src/App.test.tsx 2>/dev/null` returns nothing (file deleted)
4. `grep -r 'useTrackSelection' src/` returns zero matches
5. `npm run test` passes (all remaining Vitest tests green)
6. `npm run test:e2e` passes (all Playwright tests green)
  </verify>
  <done>
Three deprecated files deleted. No dangling imports exist. All Vitest unit tests pass. All Playwright E2E tests pass unchanged -- confirming identical UI behavior after the teardown.
  </done>
</task>

</tasks>

<verification>
Phase-level verification after both tasks complete:

1. **APP-01:** `src/App.tsx` has no useState, no useEffect, no engine hook calls, no module ID constants. Its only job is to render DawProvider wrapping Layout.
   - Verify: `grep -cE 'useState|useEffect|useToneSynth|usePanner|useTrackStrip|useLimiter|useMasterStrip|useTransportController|DEFAULT_PLAN_' src/App.tsx` returns 0

2. **APP-02:** `useAudioEngine.ts` does not exist anywhere in the codebase.
   - Verify: `find src -name 'useAudioEngine*'` returns nothing

3. **APP-03:** `useTrackSelection.ts` does not exist anywhere in the codebase and nothing imports it.
   - Verify: `find src -name 'useTrackSelection*'` returns nothing AND `grep -r 'useTrackSelection' src/` returns nothing

4. **APP-04:** `buildUiRuntime.ts` is completely unchanged (no git diff on that file).
   - Verify: `git diff src/ui-plan/buildUiRuntime.ts` shows no changes

5. **E2E regression:** All Playwright tests pass unchanged.
   - Verify: `npm run test:e2e` exits with code 0
</verification>

<success_criteria>
- App.tsx is under 20 lines: module-level DawStore creation + DawProvider + Layout
- Layout.tsx exists and contains all former App.tsx internals
- useTrackSelection.ts and its test are deleted
- App.test.tsx is deleted
- useAudioEngine.ts remains nonexistent (confirmed)
- buildUiRuntime.ts has zero modifications
- TypeScript compiles without errors
- All Vitest unit tests pass
- All Playwright E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-tsx-teardown/03-01-SUMMARY.md`
</output>
